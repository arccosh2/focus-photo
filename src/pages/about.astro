---
import BaseLayout from "src/layouts/BaseLayout.astro";
import profileIcon from "src/assets/common/profileIcon.webp";
import { css } from "styled-system/css";

const pageTitle = "About Page";
---

<BaseLayout pageTitle={pageTitle}>
  <div
    class={css({
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center",
      textAlign: { base: "left", md: "center" },
      width: "100%",
      mt: { base: "40px", lg: "32px", md: "24px", "3xl": "40px" },
      mb: { base: "72px", lg: "48px", md: "40px", "3xl": "56px" },
      padding: "0 24px",
    })}
  >
    <h1 class={css({ fontSize: { base: "lm", sm: "xl", "3xl": "xxl" } })}>
      This is the place for posting my photos.
    </h1>
    <p
      class={css({
        mt: { base: "16px", "3xl": "24px" },
        fontSize: { base: "sm", sm: "md", "3xl": "lm" },
        letterSpacing: "0.1em",
        lineHeight: "2",
      })}
    >
      I've taken photos since I was a university student.<br />The reason I
      started it was something trivial, <br />but I got interested in capturing
      the moment.<br />I'd also studied geology (espacially petrology and
      volcanology). <br />There are so many places in Japan with fascinating
      geological formations. <br />I want to capture landscapes that have
      geological significance.<br />Anyway, just look around, and enjoy my
      works!
    </p>
    <div
      class={css({
        mt: { base: "24px", md: "32px", "3xl": "56px" },
        borderRadius: "50%",
        textAlign: "center",
      })}
    >
      <img
        src={profileIcon.src}
        class={css({
          display: "inline-block",
          width: { base: "128px", "3xl": "156px" },
          borderRadius: "50%",
        })}
        height="auto"
      />
    </div>

    <p
      class={css({
        color: "profile",
        mt: { base: "16px", md: "12px" },
        fontSize: { base: "20px", "3xl": "lm" },
        fontWeight: "bold",
        fontFamily: "Verdana",
        textAlign: "center",
      })}
    >
      Haruna Nishita
    </p>
  </div>
</BaseLayout>
