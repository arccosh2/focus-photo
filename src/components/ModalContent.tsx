import { styled } from "styled-system/jsx";

export const ModalContent = styled("div", {
  base: {
    color: "text",
    backgroundColor: "card",
    borderRadius: "16px",
    padding: "1.2%",
    overflow: "scroll",
    zIndex: "contents",
  },
});
